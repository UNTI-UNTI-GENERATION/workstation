version: '3'
services:
    app:
        image: node:14.1
        ports:
            - ${NODEJS_OUTER_PORT}:${NODEJS_INNER_PORT}
        volumes:
            - ../src/:/src
        working_dir: /src
        tty: true